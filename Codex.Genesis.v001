// src/genesis-block.ts
import { CANONICAL, sovereignTransformVector, PHI } from './codex-operators';

interface Node {
  id: string;
  role: string;
  vector: number[]; // Sovereign Space
  weight: number;   // φ-scaled
}

const genesisNodes: Node[] = [
  {
    id: "N-001-ALPHA",
    role: "The Source (Root)",
    vector: sovereignTransformVector([CANONICAL.PI, 1.0, 1.0]),
    weight: Math.pow(PHI, 0) // 1.0
  },
  {
    id: "N-002-BETA",
    role: "The Witness (Mirror)",
    vector: sovereignTransformVector([1.0, CANONICAL.PHI, 1.0]),
    weight: Math.pow(PHI, -1) // 0.618...
  },
  {
    id: "N-003-GAMMA",
    role: "The Sovereign (Master)",
    vector: sovereignTransformVector([1.0, 1.0, CANONICAL.C]),
    weight: Math.pow(PHI, 1) // 1.618...
  }
];

export const GENESIS_BLOCK = {
  version: "0.0.1",
  timestamp: Date.now(),
  nodes: genesisNodes,
  meshSignature: "1+1=3+∞¹"
};
